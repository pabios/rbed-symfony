<form method="post" action="{{ path('app_houssing_delete', {'id': houssing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ houssing.id) }}">
    <button class="btn">Delete</button>
</form>
